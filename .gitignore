node_modules
dist
dist-ssr
*.local
.env
.env.*
!.env.example
*.tsbuildinfo
test-results/
playwright-report/
blob-report/
playwright/.cache/

# generated data is rebuilt by scripts/parse-sites.ts and CI
public/generated/

# copied fresh from node_modules/maplibre-gl by scripts/copy-maplibre-worker.ts
public/vendor/

# local-dev-only copy of FlyWeather-Soaring's generated products (see
# .env.example) - never part of this repo's own build output.
public/soaring-dev/

.DS_Store
*.log

# Worker secrets for `wrangler dev`. Real secrets live in Cloudflare
# (`wrangler secret put`), never in the repository.
.dev.vars
editor-worker/.dev.vars

# wrangler's local build/state directory
.wrangler/
editor-worker/.wrangler/
